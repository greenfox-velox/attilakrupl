import todo_utils
import sys
t = todo_utils()

def main():
    argList = sys.argv
    input_file = "todo.txt"
    if len(argList) == 1:
        t.welcome()
    elif argList[-1] == '-l':
        t.listToDos(input_file)
    elif len(argList) == 3:
        if argList[1] =='-a':
            t.appendTask(input_file, (argList[2]+"\n"))
        elif argList[1] =='-r':
            removeNthAndExceptions().rNAE(input_file, argList)
    elif argList[-1] == '-a':
        t.unableToAdd()
    elif argList[-1] =='-r':
        t.unableToRemove()
    else:
        unsupportedArg().uA()
            
main()