def spin_words(sentence):
	new = sentence.split()
	rev = ""
	for n in new:
		if len(n) > 5:
			for i in n[len(n)::-1]:
				rev += i
		else:
			rev += i
	return rev
    
    
print (spin_words("Malakala ha valakala"))