
from tkinter import *
import math
import random


root = Tk()

w = 1000 
canvas = Canvas(root, width = 1.5*w, height = w, bg="black")
canvas.pack()
 
def draw_branch(x, y, a):
    canvas.create_line(x, y, x, y+a, fill="gold")
    
def draw_fractal(x, y, size):
    draw_branch(x, y, size)
    small = 0.98*size
    
         
        
draw_fractal(750, 900, 50)

root.mainloop()